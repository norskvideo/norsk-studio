export default function CodecEditor(props: { width: number, height: number }) {
  // if (value.length == 0) {
  //   return <p className="node-editor-helper-text">Add some encode rungs!</p>
  // } else {
  //   return <div id={props.id}>
  //     { /* Multiselect dropdown here for enabling the rungs */}
  //   </div >
  // }
  return <>{props.width}x{props.height}</>
}
